fragment DiseaseDetails on Disease {
    id
    name
    lat
    long
    symptoms
}

query DiseasesBy($name: String!){
    allDiseases(filter: { name_contains: $name }){
        ...DiseaseDetails
    }
}
